# Data

- `returns_df_611.csv` — daily **simple returns** matrix: first column = dates, remaining columns = 611 U.S. stock tickers (2000–2018 range as used in the project). Sourced from the team’s prior embedding study / course baseline.

Use with:

`--returns-csv data/returns_df_611.csv` (from the project root).
